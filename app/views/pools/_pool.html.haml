
%ol.pool[pool]{ :class => (pool.bubble? ? 'bubble' : 'ranked' ) }
  - pool.rank_idxs.each do |rank_idx|
    = slot_tag(:seed => rank_idx, :as => :li, :class => 'slot', :id => "slot_#{pool.tournament.id}_#{rank_idx}")
