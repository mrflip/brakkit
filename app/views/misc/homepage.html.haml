.hero-unit
  %h1 Hello, world!
  %p This is your app. There are many like it but this one is yours.

  %ul
    %li= link_to 'Sign up',           new_user_registration_path
    %li= link_to 'Log in',            new_user_session_path
    %li= link_to 'Log out',           destroy_user_session_path
    %li= link_to "Sign in with Facebook", user_omniauth_authorize_path(:facebook)
    %li= link_to "Sign in with Twitter",  user_omniauth_authorize_path(:twitter)
    %li  &nbsp;
    %li= link_to 'Edit your account', edit_user_registration_path
    %li= link_to 'Show users',        user_path

.hero-unit
  = form_tag(auth_redirector_form_path(:provider => 'twitter'), :method => :get) do |f|
    %fieldset
      %legend= @title
      = text_field_tag              :login,    'mrflip',  :required => 'true', :autofocus => "autofocus"
      .actions
        = submit_tag "twitter", :class => 'login-button'
        = submit_tag "facebook", :class => 'login-button'

  %br/

  %p
    Sign up easily with Twitter, Facebook or your GMail account.
    We won't ever post anything unless you tell us to.

  %p.reveal
    
    Using a FaceSterGle account creates your profile instantly &mdash; you can
    share your brackets and see the ones by your friends.  It's for your fun and
    convenience: we'll never bug you to post anything.

.hero-unit
  %p Here are your app config settings
  %pre
    = Settings

.row
  .span8
    %h2 Simple Demo
    %p <a class='btn' href='/admin/demo'>Very simple demo of layouty things</a>
  .span8
    %h2 Bootstrap system Docs
    %p <a class='btn' href='/admin/bootstrap_demo'>Reference guide</a>


%hr/
